# Comandos para correr las imagenes de forma independiente

sudo docker run -it -p 9000:9000 sebastianmontoyaj/backcodebreakerfinal
sudo docker run -it -p 3000:3000 sebastianmontoyaj/frontcodebreakerfinal
